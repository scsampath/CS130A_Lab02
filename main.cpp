#include "Minheap.h"
#include "Hashtable.h"
#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <iterator>
#include <regex>

using namespace std;

const regex comma(",");

int main(int argc, char* argv[]) {
    // if(argc != 4) return -1;

    // int k = stoi(argv[3]);

    // // Open source file.
    // ifstream mesh(argv[1]);

    // // Here we will store the result
    // vector<string> ips;

    // // We want to read all lines of the file
    // string line{};
    // while (mesh && getline(mesh, line)) {
    //     vector<string> temp{sregex_token_iterator(line.begin(),line.end(),comma,-1), sregex_token_iterator()};
    //     ips = temp;
    // }

    // for(int i = 0; i < ips.size(); i++){
    
    // }

    // Minheap m(5);
    // m.insert();
    // m.insert();
    // m.insert();
    // m.insert();
    // m.insert();
    // // m.push();
    // // m.increment(5);
   
    // for(int i = 1; i < m.getSize(); i++){
    //     cout<< m.get(i).first<<", " << m.get(i).second <<endl;
    // }

    // // int i = 36;
    // // cout<<nextPrime(i)<<endl;

}